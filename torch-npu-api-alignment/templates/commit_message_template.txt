add validation cases for torch.<module>.<api>

PyTorch community has no direct validation cases for
torch.<module>.<api>. Add test/<path>/test_<module>.py to validate the API.
<1-2 sentences explaining what the API is and why no NPU adaptation is needed /
 what adaptation was made>

RULES FOR THIS FILE (it is reused across all 6 branches verbatim):
- Do NOT put issue numbers here as placeholders (e.g. "#xxx"). If you reference an
  issue, put the real number; otherwise omit. Inconsistent commit messages across
  branches fail cross-branch review (C2/C3).
- Keep this byte-identical across v2.7.1 / v2.9.0 / v2.10.0 / v2.11.0 / v2.12.0 / master.
- Commit with `git commit -F commit_message.txt` to avoid shell-quoting drift.
